<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="theme-color" content="#3b82f6">
    <title><%= title %></title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Font Awesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Estilos optimizados para iPhone */
        .safe-area-padding {
            padding-left: env(safe-area-inset-left);
            padding-right: env(safe-area-inset-right);
            padding-top: env(safe-area-inset-top);
            padding-bottom: env(safe-area-inset-bottom);
        }
        
        .fade-in {
            animation: fadeIn 0.5s ease-in;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .progress-fill {
            transition: width 0.3s ease-in-out;
        }
        
        .pulse-upload {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        
        /* Mejoras para touch en mobile */
        .btn-touch {
            min-height: 44px;
            min-width: 44px;
        }
        
        .input-touch {
            min-height: 48px;
            font-size: 16px; /* Previene zoom en iOS */
        }
        
        /* Scroll suave */
        html {
            scroll-behavior: smooth;
        }
        
        /* Prevenir selección de texto en botones */
        .no-select {
            -webkit-touch-callout: none;
            -webkit-user-select: none;
            -khtml-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen safe-area-padding">
    <div class="container mx-auto px-4 py-4 md:py-8">
        <!-- Header -->
        <div class="text-center mb-8 md:mb-12 fade-in">
            <h1 class="text-3xl md:text-5xl font-bold text-gray-800 mb-4">
                <i class="fas fa-qrcode text-blue-500 mr-3"></i>
                QR Generator
            </h1>
            <p class="text-lg md:text-xl text-gray-600 max-w-2xl mx-auto">
                Genera códigos QR desde Cloudinary o cualquier URL
            </p>
            
            <!-- Navigation -->
            <div class="mt-6 flex flex-col sm:flex-row justify-center space-y-2 sm:space-y-0 sm:space-x-4">
                <a href="/" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors font-medium btn-touch no-select">
                    <i class="fas fa-home mr-2"></i>Inicio
                </a>
                <a href="/files" class="bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition-colors font-medium btn-touch no-select">
                    <i class="fas fa-list mr-2"></i>Ver Archivos
                </a>
            </div>
        </div>

        <!-- Error Message -->
        <% if (error) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg mb-6 fade-in">
                <div class="flex items-center">
                    <i class="fas fa-exclamation-triangle mr-3"></i>
                    <span><%= error %></span>
                </div>
            </div>
        <% } %>

        <!-- Main Content -->
        <div class="max-w-4xl mx-auto space-y-6">
            <!-- Sección URL Personalizada -->
            <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-4 md:p-6 fade-in">
                <h2 class="text-xl md:text-2xl font-bold text-gray-800 mb-4 flex items-center">
                    <i class="fas fa-link text-purple-500 mr-3"></i>
                    Generar QR desde URL
                </h2>
                
                <form id="customUrlForm" class="space-y-4">
                    <div class="flex flex-col sm:flex-row space-y-2 sm:space-y-0 sm:space-x-2">
                        <input 
                            type="url" 
                            name="customUrl" 
                            id="customUrlInput"
                            placeholder="https://ejemplo.com o ejemplo.com"
                            class="flex-1 px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-200 input-touch"
                            pattern="https?://.+"
                            title="Ingresa una URL válida"
                        >
                        <button 
                            type="submit"
                            id="generateCustomQRBtn"
                            class="bg-purple-600 hover:bg-purple-700 text-white font-medium py-3 px-6 rounded-lg transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:ring-opacity-50 btn-touch no-select"
                        >
                            <i class="fas fa-qrcode mr-2"></i>
                            Generar QR
                        </button>
                    </div>
                    <p class="text-sm text-gray-500 text-center">
                        Soporta cualquier URL: sitios web, enlaces, etc.
                    </p>
                </form>
            </div>

            <!-- Upload Section -->
            <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-4 md:p-6 fade-in">
                <h2 class="text-xl md:text-2xl font-bold text-gray-800 mb-4 flex items-center">
                    <i class="fas fa-cloud-upload-alt text-blue-500 mr-3"></i>
                    Subir a Cloudinary
                </h2>
                
                <form id="uploadForm" enctype="multipart/form-data" class="space-y-6">
                    <!-- File Input -->
                    <div class="border-2 border-dashed border-gray-300 rounded-xl p-6 md:p-8 text-center transition-colors hover:border-blue-400 cursor-pointer btn-touch" 
                         onclick="document.getElementById('fileInput').click()">
                        <i class="fas fa-cloud-upload-alt text-3xl md:text-4xl text-gray-400 mb-4"></i>
                        <input 
                            type="file" 
                            name="file" 
                            id="fileInput" 
                            class="hidden" 
                            accept="image/*,video/*,.pdf,.doc,.docx,.txt,.xls,.xlsx"
                        >
                        <div>
                            <p class="text-base md:text-lg text-gray-600 mb-2">Toca para seleccionar un archivo</p>
                            <p class="text-xs md:text-sm text-gray-500">Imágenes, Videos, PDF, Word, Excel</p>
                            <p class="text-xs md:text-sm text-gray-500 mt-1">Máximo: 50MB</p>
                        </div>
                    </div>

                    <!-- File Info -->
                    <div id="fileInfo" class="hidden p-4 bg-blue-50 rounded-lg border border-blue-200">
                        <div class="flex items-center justify-between">
                            <div class="flex items-center space-x-3">
                                <i class="fas fa-file text-blue-500 text-lg md:text-xl"></i>
                                <div>
                                    <p id="fileName" class="font-semibold text-gray-800 text-sm md:text-base"></p>
                                    <p id="fileSize" class="text-xs md:text-sm text-gray-600"></p>
                                </div>
                            </div>
                            <button type="button" onclick="clearFile()" class="text-red-500 hover:text-red-700 btn-touch">
                                <i class="fas fa-times"></i>
                            </button>
                        </div>
                    </div>

                    <!-- Upload Button -->
                    <button 
                        type="submit" 
                        id="uploadBtn"
                        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-4 rounded-lg transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 text-base md:text-lg btn-touch no-select hidden"
                    >
                        <i class="fas fa-cloud-upload-alt mr-2"></i>
                        Subir a Cloudinary
                    </button>

                    <!-- Progress Bar -->
                    <div id="progressSection" class="hidden space-y-2">
                        <div class="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
                            <div id="progressFill" class="bg-blue-600 h-2 rounded-full progress-fill w-0"></div>
                        </div>
                        <p id="progressText" class="text-sm text-gray-600 text-center">Preparando subida...</p>
                    </div>
                </form>
            </div>

            <!-- Results Section -->
            <div id="resultsSection" class="hidden fade-in">
                <!-- Cloudinary URL -->
                <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-4 md:p-6 mb-6">
                    <h2 class="text-xl md:text-2xl font-bold text-gray-800 mb-4 flex items-center">
                        <i class="fas fa-link text-green-500 mr-3"></i>
                        URL Generada
                    </h2>
                    
                    <div class="space-y-4">
                        <div class="flex flex-col sm:flex-row space-y-2 sm:space-y-0 sm:space-x-2">
                            <input 
                                type="text" 
                                id="urlInput"
                                readonly
                                class="flex-1 px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent bg-gray-50 input-touch text-sm md:text-base"
                                placeholder="La URL aparecerá aquí..."
                            >
                            <button 
                                onclick="copyUrl()" 
                                id="copyUrlBtn"
                                class="bg-gray-600 hover:bg-gray-700 text-white font-medium py-3 px-4 sm:px-6 rounded-lg transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-opacity-50 btn-touch no-select"
                            >
                                <i class="fas fa-copy mr-2"></i>Copiar
                            </button>
                        </div>
                        
                        <button 
                            onclick="generateQR()" 
                            id="generateQRBtn"
                            class="w-full bg-green-600 hover:bg-green-700 text-white font-medium py-3 px-4 rounded-lg transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-opacity-50 text-base md:text-lg btn-touch no-select"
                        >
                            <i class="fas fa-qrcode mr-2"></i>
                            Generar Código QR
                        </button>
                    </div>
                </div>

                <!-- QR Code Section -->
                <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-4 md:p-6">
                    <h2 class="text-xl md:text-2xl font-bold text-gray-800 mb-4 md:mb-6 flex items-center">
                        <i class="fas fa-qrcode text-purple-500 mr-3"></i>
                        Código QR
                    </h2>
                    
                    <div class="text-center space-y-6">
                        <div id="qrContainer" class="hidden">
                            <div class="bg-gray-50 rounded-lg p-4 md:p-6 inline-block max-w-full">
                                <img id="qrImage" src="" alt="Código QR" class="mx-auto max-w-full h-auto" style="max-width: 280px;">
                            </div>
                        </div>
                        
                        <div id="noQR" class="text-gray-500 py-6 md:py-8">
                            <i class="fas fa-qrcode text-3xl md:text-4xl mb-4"></i>
                            <p class="text-sm md:text-base">El código QR aparecerá aquí</p>
                        </div>
                        
                        <button 
                            onclick="downloadQR()" 
                            id="downloadQRBtn"
                            class="bg-purple-600 hover:bg-purple-700 text-white font-medium py-3 px-6 rounded-lg transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:ring-opacity-50 btn-touch no-select hidden"
                        >
                            <i class="fas fa-download mr-2"></i>
                            Descargar QR
                        </button>
                    </div>
                </div>
            </div>

            <!-- Uploaded File Info -->
            <% if (uploadedFile) { %>
                <div class="bg-green-50 border border-green-200 rounded-lg p-4 fade-in">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-3">
                            <i class="fas fa-check-circle text-green-500 text-lg md:text-xl"></i>
                            <div>
                                <p class="font-semibold text-green-800 text-sm md:text-base">¡Archivo subido exitosamente!</p>
                                <p class="text-xs md:text-sm text-green-600">
                                    <%= uploadedFile.name %> • <%= uploadedFile.size %> • <%= uploadedFile.type %>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>

    
    </div>

    <!-- JavaScript -->
    <script src="/js/main.js"></script>
</body>
</html>